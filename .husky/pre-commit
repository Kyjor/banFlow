#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit typecheck..."

if ! npm run lint; then
  echo "❌ Typecheck failed. Please fix the errors before committing."
  exit 1
fi

echo "✅ Typecheck passed! Proceeding with commit..."
